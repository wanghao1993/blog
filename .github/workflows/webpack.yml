name: NodeJS with Webpack

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x]
    
    steps:
    - uses: actions/checkout@v2

    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}

    - name: Build
      run: |
        yarn
        yarn docs:build
        
    - name: Deploy to Staging server
      uses: easingthemes/ssh-deploy@v2.0.7
      env:
        SSH_PRIVATE_KEY: ${{ tencent.SERVER_SSH_KEY }}
        SOURCE: "docs/.vitepress/dist/"
        REMOTE_HOST: ${{ tencent.DEPLOY_HOST }}
        REMOTE_USER: ${{ tencent.DEPLOY_USER }}
        TARGET: ${{ tencent.DEPLOY_REMOTE_DIR }}
